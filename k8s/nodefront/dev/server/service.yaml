apiVersion: v1
kind: Service
metadata:
  name: nodedeploy-server
  namespace: storefront-devnode
  labels:
    app.kubernetes.io/name: nodedeploy-server
    app.kubernetes.io/managed-by: deepagent
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: nodedeploy-server
  ports:
    - name: http
      port: 80
      targetPort: 3000
